<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}{{ societe.societe_libelle }}{% endblock %}</title>

        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>

        {% include 'partials/_header.html.twig' %}

        <main>
            {% block body %}{% endblock %}
        </main>

        {% include 'partials/_footer.html.twig' %}

    </body>
</html>
